import { createLogger } from '@kurrent-ui/utils';

export const logger = createLogger(
    'Router demo',
    'radial-gradient( circle 489px at 49.3% 46.6%,  rgba(255,214,126,1) 0%, rgba(253,200,0,1) 100.2% )',
);
