{
    "name": "stores-demo",
    "version": "0.1.0-alpha",
    "private": true,
    "description": "Demo for @eventstore-ui/stores",
    "scripts": {
        "build": "stencil build",
        "start": "stencil build --dev --watch --serve --no-open",
        "test": "stencil test --spec --passWithNoTests",
        "test.watch": "stencil test --spec --watchAll",
        "lint": "yarn g:lint",
        "fix": "yarn g:fix"
    },
    "dependencies": {
        "@eventstore-ui/stores": "workspace:*",
        "@eventstore-ui/utils": "workspace:*"
    },
    "devDependencies": {
        "@stencil/core": "^4.0.2"
    }
}
