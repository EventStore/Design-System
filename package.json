{
    "private": true,
    "version": "0.0.2",
    "repository": {
        "type": "git",
        "url": "ssh://git@github.com:EventStore/Design-System.git"
    },
    "workspaces": [
        "packages/*",
        "tools/*"
    ],
    "prettier": "@eventstore/configs/prettier",
    "scripts": {
        "build": "yarn workspaces run build",
        "test": "yarn workspaces run test"
    },
    "devDependencies": {
        "@eventstore/configs": "0.0.1"
    }
}
