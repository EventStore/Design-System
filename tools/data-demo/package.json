{
    "name": "data-demo",
    "version": "0.0.58",
    "private": true,
    "description": "Demo for @eventstore/data",
    "scripts": {
        "build": "stencil build",
        "start": "stencil build --dev --watch --serve --no-open",
        "test": "stencil test --spec --passWithNoTests",
        "test.watch": "stencil test --spec --watchAll",
        "lint": "prettier --check \"src/**/**/!(*.d).{ts,tsx,css,json,html}\""
    },
    "dependencies": {
        "@eventstore/data": "^0.0.58"
    },
    "devDependencies": {
        "@stencil/core": "^2.3.0"
    }
}
