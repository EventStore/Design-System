{
    "name": "router-demo",
    "version": "0.0.1",
    "private": true,
    "description": "Demo for @eventstore/router",
    "scripts": {
        "build": "stencil build",
        "start": "stencil build --dev --watch --serve --no-open",
        "test": "jest",
        "test.watch": "jest --watch"
    },
    "dependencies": {
        "@eventstore/router": "0.0.1"
    },
    "devDependencies": {
        "@stencil/core": "^1.15.0"
    }
}
