{
    "name": "stores-demo",
    "version": "0.0.62",
    "private": true,
    "description": "Demo for @eventstore/stores",
    "scripts": {
        "build": "stencil build",
        "start": "stencil build --dev --watch --serve --no-open",
        "test": "stencil test --spec --passWithNoTests",
        "test.watch": "stencil test --spec --watchAll",
        "lint": "prettier --check \"src/**/**/!(*.d).{ts,tsx,css,json,html}\""
    },
    "dependencies": {
        "@eventstore/stores": "^0.0.62",
        "@eventstore/utils": "^0.0.62"
    },
    "devDependencies": {
        "@stencil/core": "^2.7.1"
    }
}
